#pragma once
#include <stdbool.h>
#include <stddef.h>
#include <stdio.h>

void print_arr(const int *arr, size_t size);
extern int arr_pequenio[];
extern int arr_ordenado_pequenio[];
extern size_t size;
extern int target;
